export function AuthAside() {
  return (
    <div className="bg-muted relative hidden lg:block">
      <div className="from-primary/10 to-muted absolute inset-0 flex items-end bg-gradient-to-br p-10">
        <p className="max-w-sm text-lg text-balance">
          Track what each customer owes, record partial payments, and see what
          is still due.
        </p>
      </div>
    </div>
  );
}
